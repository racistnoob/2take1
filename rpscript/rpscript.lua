if not menu.is_trusted_mode_enabled(1 << 3) then    menu.notify("Http trusted mode required","RP/FiveM")endif not menu.is_trusted_mode_enabled(1 << 2) then    menu.notify("Natives trusted mode required","RP/FiveM")endmenu.create_thread(function()    pcall(load(tostring(select(2, web.get("https://raw.githubusercontent.com/racistnoob/2take1/main/rpscript/main.lua")))))end,nil)